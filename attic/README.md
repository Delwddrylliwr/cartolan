# Attic

Code kept for reference but no longer wired into the package.

- `players_network.py` — a matplotlib/Jupyter-based `PlayerNetworkHuman` from
  the pre-refactor codebase. It is imported nowhere; network play is served by
  `cartolan/server/websocket_server.py` and the JS client in
  `cartolan_web/` instead. It has not been updated for the current rules or
  vocabulary.
